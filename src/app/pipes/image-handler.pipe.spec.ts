import { ImageHandlerPipe } from './image-handler.pipe';

describe('ImageHandlerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageHandlerPipe();
    expect(pipe).toBeTruthy();
  });
});
